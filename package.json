{
  "name": "shift-todo",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://hrirkslab.github.io/shift-todo",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "react-scripts": "4.0.3"
  }
}
